실행


AIzaSyC6FwhsVXDSTRCha_TzLK4z7B7GxHWETMU
$env:GEMINI_API_KEY = "AIzaSyC6FwhsVXDSTRCha_TzLK4z7B7GxHWETMU"
python run_causal.py


cd macro-graph/backend
python -m uvicorn api:app --host 0.0.0.0 --port 8000

============================================================
매크로 이벤트 지식 그래프 — 인수인계서 v3
작성일: 2026-03-16
이전 인수인계서: HANDOVER_v2.txt
============================================================


1. 프로젝트 한 줄 요약
------------------------------------------------------------
뉴스를 수집해 Gemini로 인과관계를 추출하고,
"전쟁이 나면 유가/금리/공급망이 어떻게 연결되나?"를
그래프 + 인과 탐색기로 시각화하는 매크로 분석 웹앱.


2. v3 추가된 것 (v2 대비)
------------------------------------------------------------
[신규 파일]
  causal_routes.py       # 인과 탐색기 전용 API 라우터 (그래프 필터 우회)
  historical_crawler.py  # 날짜 범위 + 검색어 기반 과거 기사 수집

[신규 페이지]
  frontend/dist/causal_explorer.html  # 인과 탐색기 (시나리오 검색 UI)

[변경된 파일]
  api.py         # causal_routes 라우터 2줄 추가 (44~45번째 줄)
  frontend/dist/index.html  # 네비게이션에 "인과 탐색기" 버튼 추가

[신규 API 엔드포인트]
  GET /api/causal/search          — 인과 엣지 전문 검색 (그래프 우회)
  GET /api/causal/scenario        — 시나리오 영향 트리 ("X가 발생하면?")
  GET /api/causal/chains/search   — 인과 체인 텍스트 검색
  GET /api/causal/explorer/stats  — 탐색기 전용 통계


3. 디렉토리 구조 (현재)
------------------------------------------------------------
macro-graph/
├── backend/
│   ├── api.py                  # FastAPI 서버 — 모든 엔드포인트
│   ├── causal_routes.py        # 인과 탐색기 전용 라우터 ★ 신규
│   ├── historical_crawler.py   # 과거 기사 수집 크롤러 ★ 신규
│   ├── crawler.py              # 뉴스 크롤러 (RSS 22개 + BS 4개 + URL파일)
│   ├── run_causal.py           # 인과 체인 추출 실행기 (한도 관리 포함)
│   ├── causal_extractor.py     # Gemini 인과관계 추출 엔진
│   ├── noun_extractor.py       # 명사 추출 + STOPWORDS + Gemini 노이즈 필터
│   ├── graph_builder.py        # 공동출현 그래프 + 멀티홉 경로 탐색
│   ├── concept_normalizer.py   # 인과 노드 정규화 (유사어 통합)
│   ├── database.py             # SQLite 스키마 + 연결 관리
│   ├── price_engine.py         # yfinance/pykrx 가격 수집
│   ├── ingest_news_folder.py   # news/ 폴더 txt 파일 수동 입력
│   ├── macro_graph.db          # SQLite DB (자동 생성, 현재 47MB)
│   ├── crawler.log             # 크롤링 로그
│   ├── urls.txt                # 수동 크롤링할 URL 목록
│   └── news/                   # 수동 입력 기사 txt 파일 폴더
└── frontend/
    └── dist/
        ├── index.html              # D3.js Force Graph 메인 그래프 UI
        └── causal_explorer.html    # 인과 탐색기 UI ★ 신규


4. 서버 실행
------------------------------------------------------------
cd macro-graph/backend
export GEMINI_API_KEY=your_key_here
python -m uvicorn api:app --host 0.0.0.0 --port 8000

브라우저:
  http://localhost:8000                   # 메인 그래프
  http://localhost:8000/causal_explorer.html  # 인과 탐색기


5. ★ 매일 해야 하는 것 (일일 루틴)
------------------------------------------------------------
순서대로 실행. 총 소요 시간: 약 1시간 20분 (기사 수에 따라 변동)

  [1단계] 뉴스 크롤링 — 약 5~10분
  python crawler.py

  [2단계] 인과 체인 추출 (Gemini) — 약 58분 ★ 핵심
  python run_causal.py
  → Gemini 무료 한도: 하루 250회 (run_causal.py가 자동 관리)
  → Ctrl+C로 중단해도 다음 실행 시 이어서 진행
  → 재실행해도 이미 처리한 기사는 자동으로 건너뜀

  [3단계] 공동출현 그래프 재빌드 — 약 1~2분
  python graph_builder.py

  ※ noun_extractor.py 는 따로 안 돌려도 됨
     causal_extractor가 체인 노드를 article_nouns에 자동 저장함

권장 실행 시각: 장 마감 후 (21:00~)
  → 하루 치 뉴스가 다 올라온 시점
  → Gemini API 사용량이 자정에 리셋되므로 저녁에 돌리면 효율적

또는 API로 한 번에:
  POST http://localhost:8000/api/pipeline/full
  POST http://localhost:8000/api/pipeline/extract-causality


6. 가끔 해도 되는 것 (주 1~2회)
------------------------------------------------------------
[과거 기사 보충 수집]
  python historical_crawler.py --days 7 --keyword "금리"
  python historical_crawler.py --days 7 --sources yna_archive,mk_archive
  → 소스 목록: python historical_crawler.py --list-sources
  → 날짜 범위: --from 2026-03-01 --to 2026-03-15

[가격 데이터 수집]
  POST http://localhost:8000/api/pipeline/fetch-prices
  POST http://localhost:8000/api/pipeline/detect-spikes

[인과 노드 정규화 — 유사어 통합]
  POST http://localhost:8000/api/pipeline/extract
  → "삼성전자" / "삼성" / "삼성전자(005930)" 같은 유사어를 하나로 통합


7. 분석할 때 쓰는 API
------------------------------------------------------------
★ 인과 탐색기 (UI)
  http://localhost:8000/causal_explorer.html
  → "전쟁", "금리 인상", "유가 급등" 등 검색하면
    시나리오 영향 트리(순방향/역방향) + 인과 체인 전체 조회 가능
  → 그래프 IDF 필터에서 제외된 2,200+ 개 노드까지 전부 검색됨

★ 시나리오 분석 — "X가 발생하면 무엇이 영향받나?"
  GET /api/causal/scenario?q=전쟁&depth=3
  GET /api/causal/scenario?q=유가+급등&depth=4
  GET /api/causal/impact/중동전쟁?depth=4    (기존 엔드포인트)

★ 경로 탐색 — "A가 B에 어떻게 연결되나?"
  GET /api/causal/path?node_a=후티&node_b=인플레

★ 인과 엣지 전문 검색 (그래프 우회)
  GET /api/causal/search?q=반도체&category=기술산업
  GET /api/causal/search?q=유가&min_strength=2

★ 카테고리별 체인 조회
  GET /api/causal/categories
  GET /api/causal/chains/지정학
  GET /api/causal/top-chains
  GET /api/causal/chains/search?q=전쟁&category=안보

★ DB 통계
  GET /api/causal/explorer/stats   # 탐색기용 (그래프 우회)
  GET /api/stats                   # 전체 통계


8. 현재 DB 현황 (2026-03-16 기준)
------------------------------------------------------------
  수집 기사     : 3,182건 (연합뉴스 1,609 / 한경 687 / 매경 508 / 이인포맥스 336)
  인과 엣지     : 1,903건
  인과 체인     : 776건
  인과 고유 노드: 2,277개
  그래프 노드   : 40개 (IDF 60% 필터 이후)
  가격 데이터   : 975건
  DB 크기       : 47MB


9. 핵심 설정값 위치
------------------------------------------------------------
[GEMINI_API_KEY]
  환경변수: export GEMINI_API_KEY=...
  api.py 16번째 줄에 하드코딩도 가능 (보안상 권장 안 함)

[Gemini 일일 한도 조정]
  run_causal.py → DAILY_LIMIT = 245  (250 RPD 중 여유 5개)
  run_causal.py → RATE_LIMIT_SEC = 7.0  (10 RPM 기준, 여유 1초)

[STOPWORDS 확장 — 노이즈 단어 추가]
  noun_extractor.py → STOPWORDS set

[FINANCE_KEYWORDS 확장 — 무조건 유지할 단어]
  noun_extractor.py → FINANCE_KEYWORDS set

[자산 노드 추가 — 가격 추적 종목]
  price_engine.py → ASSET_NODES dict

[IDF 노이즈 상한 조정]
  graph_builder.py → MAX_IDF_RATIO = 0.60
  → 그래프 노드 수가 너무 적으면 0.75~0.80으로 높이기

[과거 기사 수집 소스 추가]
  historical_crawler.py → SEARCH_SOURCES dict


10. 알려진 제한사항
------------------------------------------------------------
[네이버 뉴스 봇 차단]
  VM/서버 환경에서 HTTP 요청 차단
  → 대안: news/ 폴더에 txt 파일로 수동 입력

[Gemini 모델명]
  noun_extractor.py  : "gemini-2.0-flash" (노이즈 필터)
  causal_extractor.py: "gemini-2.0-flash" (인과 추출)
  api.py (explain)   : "gemini-2.5-flash" (관계 설명)
  API 키 만료 시 aistudio.google.com/apikey 에서 갱신

[IDF 상한 60% — 그래프 노드 수 부족]
  기사 수 대비 그래프 노드가 너무 적으면 MAX_IDF_RATIO 상향 조정

[인과 노드 정규화 미완성]
  "삼성전자 매력 상승" / "삼성전자 관심" 같은 유사 노드가 별개로 쪼개짐
  → 체인이 끊어지는 원인
  → concept_normalizer.py 가 존재하지만 파이프라인에 완전 통합 안 됨


11. 남은 개선 과제
------------------------------------------------------------
[A] 인과 노드 정규화 파이프라인 통합
  cause/effect 에 등장하는 유사어를 FINANCE_KEYWORDS 기반으로 통합

[B] 미국 주식 뉴스 크롤링
  Reuters, CNBC, MarketWatch 셀렉터 추가 (Bloomberg/WSJ는 페이월)
  historical_crawler.py SEARCH_SOURCES 에 추가

[C] TF-IDF / Burst 기반 중요도 스코어
  nodes 테이블에 importance = burst_score × IDF 컬럼 추가
  → "저빈도-고임팩트" 이벤트 노드가 그래프 상위에 노출됨

[D] 인과 그래프 시각화
  현재: 패널 텍스트 목록
  개선: 방향성 D3 DAG (화살표 있는 그래프)
  → causal_explorer.html 에 시각화 탭 추가

[E] 정기 자동 실행
  매일 21:00 이후 아래 순서로 자동화:
    python crawler.py
    python run_causal.py
    python graph_builder.py


12. 라이브러리 설치
------------------------------------------------------------
pip install fastapi uvicorn kiwipiepy networkx \
    beautifulsoup4 lxml feedparser requests \
    pykrx yfinance google-genai python-dotenv


============================================================
끝.
============================================================